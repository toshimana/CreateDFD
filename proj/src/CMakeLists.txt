PROJECT( CreateDFD )

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

# Boost
FIND_PACKAGE( Boost REQUIRED )
IF ( Boost_FOUND )
  INCLUDE_DIRECTORIES( ${Boost_INCLUDE_DIR} )
ENDIF()

# Target
FILE( GLOB SRCS "*.cpp" )
ADD_LIBRARY( CreateDFD STATIC ${SRCS} )

